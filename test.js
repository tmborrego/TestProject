//no need to add script tags when writing in js

console.log('hi there');

/* variables = var (not used anymore), let, const
   use const, unless you know you will reassign the value
   let allows you to reassign the value
*/

//data types: strings, numbers, boolean, null, undefined, symbol

const name = 'TB';
const age = 41;
const rating = 8.5;
const isCool = true;
const x=null;
const y=undefined;
//what type of data exists? use console.log below
console.log(typeof isCool) 
//old concatenation
console.log (name + age)
//modern concatenation -Template strings
console.log (`My name is ${name} and I am ${age}`);



